# Security and Data Boundary

This public proof is documentation-first and intentionally excludes production secrets, credentials, customer records and live integration configuration.

The linked demonstration uses synthetic data. Do not enter passenger identities, authentication secrets, card data, confidential supplier pricing or other sensitive information into the demo.

Any future executable-source release requires a separate source review before publication.
